# iam_scout — Status Snapshot (2026-07-10)

Read-only audit. No code, docs, or repo state modified as part of producing this file.

## Repo Structure

```
.archive/2026-07-09-export-entra-app-registrations-original.ps1   (gitignored convenience copy, not rollback mechanism)
.claude/hooks/protect-claude-md.sh
.claude/settings.json                  (tracked)
.claude/settings.local.json            (gitignored, per-dev)
CLAUDE.md
docs/ENTRAID_POWERSHELL_PROJECT_PLAN.md
docs/IAM_SCOUT_LEARNING_LOOP.md
docs/LEARNINGS.md
docs/README.md
docs/status/STATUS_2026-07-09.md       (present in git history, deleted in working tree — see Flags)
entra-scripts/Export-EntraAppRegistrations.ps1        (601 lines, Phase 1 original)
entra-scripts/export-entra-app-registrations-v2.ps1   (503 lines, Phase 1 expanded, uses auth module)
entra-scripts/modules/iam-scout-graph-auth/iam-scout-graph-auth.psm1  (366 lines)
entra-scripts/modules/iam-scout-graph-auth/iam-scout-graph-auth.psd1
output/.gitkeep                        (real dir now exists, tracked via .gitkeep)
requirements.psd1
```

No stub/placeholder files. No empty directories left over from earlier snapshots (the "`entra-scripts/` empty" and "no `output/` dir" gaps noted in the 2026-07-09 snapshot are both resolved).

Two Phase-1 scripts now coexist:
- `Export-EntraAppRegistrations.ps1` — original, self-contained (auth + export logic inline), 6-field export. Left untouched as reference per commit history.
- `export-entra-app-registrations-v2.ps1` — imports the new `iam-scout-graph-auth` module, exports the full 39-property `application` schema across a multi-sheet workbook (`Core` + up to 6 collection sheets).

Both are live, working scripts — the repo has not yet consolidated to a single canonical Phase 1 script (see Flags).

## Plan vs. Reality

Source: `docs/ENTRAID_POWERSHELL_PROJECT_PLAN.md` (dated 2026-07-09, not updated since).

- **Phase 0 (repo hygiene):** All checkboxes marked done except one explicitly left open — "`output/` exists and is gitignored, but the script's `-OutputDirectory` default still points at the current directory." That's still true in both scripts today (`$OutputDirectory = (Get-Location).Path`). Accurately reflected as open.
- **Phase 1 (app registration export):** Plan marks this "COMPLETE" and describes only the original 6-field script. Reality has moved past that: the auth-module extraction (commit `c3ed7ba`) and the full-schema/multi-sheet expansion (commit `8851778`) both happened *after* the plan doc was last touched. Neither is mentioned in the plan. This is **documented-but-stale**, not scope creep — both changes stay within Phase 1's app-registration-only boundary (confirmed below), they just aren't reflected in the plan file yet.
- **Phase 2 (enterprise apps / secrets metadata / log data):** Plan says scope TBD, not started. Reality matches — no `Get-MgServicePrincipal`, `Get-MgAuditLog*`, or `Get-MgReport*` calls anywhere in the repo (confirmed via grep, zero matches).
- **Out-of-scope items (AWS/FastAPI/React/SQLite):** None present in the repo, consistent with "parked."
- No scope creep found: everything implemented maps to a Phase 0 or Phase 1 line item.

## Recent Activity

Last 10 commits (since the 2026-07-09 status snapshot, which predates git init and thus isn't a diffable baseline):

1. **Schema expansion** (`8851778`) — `export-entra-app-registrations-v2.ps1` expanded from 6 fields to the full 39-property `application` resource, multi-sheet `.xlsx` output (`Core` + 6 conditional collection sheets), secret-value-safety re-verified against Graph docs.
2. **Auth module extraction** (`c3ed7ba`) — Graph app-only auth, DPAPI credential store, and module-install logic pulled out of the original script into `entra-scripts/modules/iam-scout-graph-auth/`, consumed by the new v2 script.
3. **CLAUDE.md self-edit protection** (`e2da90c`, `773f62a`, `3f46e8a`) — added a PreToolUse hook blocking `Edit`/`Write`/`MultiEdit` against any file named `CLAUDE.md`; a follow-up commit fixed the hook script's git-recorded file mode (was `100644`, should be `100755`); a third commit logged an end-to-end re-audit of the hook.
4. Earlier in the window: `docs/README.md` index creation, initial `CLAUDE.md`/`.gitignore`/`requirements.psd1`, and the first git commit itself (repo had no git history at all before 2026-07-09).

Two rollback tags exist and match the stated convention: `pre-graph-auth-module`, `pre-app-registration-expand`.

## Open Items

- `-OutputDirectory` default (current directory vs. `output/`) — explicitly left as an open decision in the plan, unchanged.
- Two parallel Phase 1 scripts (original + v2) — no stated plan for whether v2 replaces the original, both stay, or the original is archived.
- Plan doc (`ENTRAID_POWERSHELL_PROJECT_PLAN.md`) not updated to reflect the auth-module extraction or schema expansion, both already shipped and validated.
- `docs/LEARNINGS.md` has two "Candidate for CLAUDE.md: Yes" entries (MCP batching rule, hook-enforcement note) that per the file's own header are "not yet applied" pending review/approval — still outstanding.
- Open questions logged in the 2026-07-09 hook-audit entry, unresolved: (a) should the hook match `CLAUDE.md` by basename anywhere in the repo or by exact root path only; (b) status of a previously-flagged `docs/CLAUDE.md` (confirmed not to currently exist, cause unclear).

## PowerShell/Entra Phase 1 Check

Confirmed via direct grep across `entra-scripts/` (both scripts + the auth module) — **zero** matches for:
- `Get-MgServicePrincipal` / any enterprise-app or service-principal cmdlet
- `Get-MgAuditLog*` / `Get-MgReport*` / any sign-in or audit log cmdlet
- Any code path that reads back a `passwordCredentials.secretText` or `keyCredentials.key` value (v2's `PasswordCredentials` sheet only has `KeyId`, `CredentialDisplayName`, `Hint`, `StartDateTime`, `EndDateTime`; `KeyCredentials` sheet excludes the raw `key` property entirely, per LEARNINGS rationale — Graph never returns it on a list call anyway)

Both scripts remain scoped to app registrations (the `application` resource) only. Phase 1 boundary is intact.

## Flags for Review

1. **Uncommitted deletion:** `docs/status/STATUS_2026-07-09.md` shows as deleted in the working tree (`git status`) but is still present in git history at `HEAD`. Not restored or removed as part of this audit (read-only) — flagging so it isn't lost by accident if unintended.
2. **Hook file-mode history is confusing but currently correct:** the most recent LEARNINGS entry ("End-to-end audit...") reports the hook script's git-recorded mode as `100644` and explicitly defers fixing it. Current `git ls-files -s` shows `100755` — the fix commit (`773f62a fix: restore executable bit...`) actually landed *before* that audit-log commit in the log, so the concern is already resolved in the repo; the LEARNINGS narrative just reads as if it's still open. No action needed, but worth a one-line follow-up note in LEARNINGS.md so a future read doesn't re-flag it as outstanding.
3. **Two Phase 1 scripts, no consolidation decision recorded** (see Open Items) — worth an explicit call on whether the original `Export-EntraAppRegistrations.ps1` becomes purely historical/archived or stays a supported entry point.
4. **`docs/ENTRAID_POWERSHELL_PROJECT_PLAN.md` drift** — two shipped, validated changes (auth module, schema expansion) postdate the plan doc and aren't reflected in it. Low risk (not scope creep, just staleness) but the plan will keep drifting further if it isn't touched alongside future `entra-scripts/` work.
