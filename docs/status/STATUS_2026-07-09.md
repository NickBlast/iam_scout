# iam_scout — Status Snapshot (2026-07-09)

## Repo Structure

Not a git repository (`git status` fails with "not a git repository"). Full file listing, excluding nothing since the tree is already tiny:

```
.claude/settings.local.json
Export-EntraAppRegistrations.ps1
EntraAppRegistrations_20260709_144317.xlsx
entra-scripts/                     (empty directory)
```

- **Export-EntraAppRegistrations.ps1** — fully implemented, single-file PowerShell 7 script. Connects to Microsoft Graph app-only, pulls app registrations, exports to `.xlsx`. No stubs or TODOs inside it.
- **EntraAppRegistrations_20260709_144317.xlsx** — a generated output artifact from running the script (timestamped filename matches the script's naming convention), sitting in the repo root rather than gitignored/excluded.
- **entra-scripts/** — empty directory, no files. Presumably intended to hold future scripts but nothing has been added yet.
- **.claude/settings.local.json** — grants permission for two Microsoft Learn MCP tools (`microsoft_docs_search`, `microsoft_code_sample_search`). Consistent with the script's header claiming its DPAPI approach was "verified against Microsoft Learn."
- No `IAM_SCOUT_PROJECT_PLAN.md`, no `CLAUDE.md`, no `README`, no `requirements.txt`/`package.json`/`.psd1` manifest, no test files.

## Plan vs. Reality

Neither `IAM_SCOUT_PROJECT_PLAN.md` nor `CLAUDE.md` exist anywhere in the repo, so there is no documented phase plan or scope definition to compare against. This comparison cannot be performed — flagged below for review.

## Recent Activity

No git history available (repo is not initialized under git). File modification times are the only signal:
- `entra-scripts/` created 2026-07-07
- `Export-EntraAppRegistrations.ps1` last modified 2026-07-09 14:39
- `EntraAppRegistrations_20260709_144317.xlsx` generated 2026-07-09 14:43 (a live run's output, 4 minutes after the script's last edit)
- `.claude/settings.local.json` created 2026-07-09 14:06

## Open Items

- No TODO/FIXME/NotImplemented markers found anywhere in the codebase.
- No incomplete function bodies found — every function in `Export-EntraAppRegistrations.ps1` has a full implementation with error handling.
- `entra-scripts/` is empty — unclear what it's meant to hold; may be a placeholder for future script organization.

## PowerShell/Entra Phase 1 Check

`Export-EntraAppRegistrations.ps1` reviewed line-by-line against the stated Phase 1 boundary (app registrations only — no enterprise apps, no secrets, no log data):

- **Scope confirmed as app registrations only.** Uses `Get-MgApplication` (the `application` resource), explicitly documented in the header as "NOT enterprise apps / service principals." No calls to `Get-MgServicePrincipal` or any enterprise-app/service-principal cmdlets appear anywhere in the file.
- **No secrets are read or exported.** The only "secret" handling is the script's own client-secret credential used to authenticate to Graph (stored DPAPI-encrypted locally, never logged or exported). No application client secrets/certificates belonging to the *scanned* app registrations are retrieved or written to the output file.
- **No log/sign-in data.** No calls to sign-in logs, audit logs, or `Get-MgAuditLog*`/`Get-MgReport*` cmdlets.
- **Exported fields** are limited to: `DisplayName`, `AppId`, `Id`, `SignInAudience`, `CreatedDateTime`, `PublisherDomain`, and optionally `Owners` (via `-IncludeOwners`, an opt-in switch that adds owner UPNs/display names — still app-registration metadata, not secrets or logs).
- **Conclusion: still within Phase-1-style boundaries** (app registrations only, no enterprise apps, no secrets, no log data) — though this can't be checked against an actual written Phase 1 definition since no plan document exists in the repo.

## Flags for Review

1. **No git repository.** There is no version history, no commit log, and no way to track changes over time. If this project is meant to be tracked, it needs `git init` and an initial commit.
2. **No `IAM_SCOUT_PROJECT_PLAN.md` or `CLAUDE.md`.** The audit requested in this task assumes these exist; they don't. Phase boundaries, scope, and "done"/"in progress" status can't be verified against anything — they may exist only in conversation/memory, or may not have been written yet.
3. **Generated output file committed alongside source.** `EntraAppRegistrations_20260709_144317.xlsx` (a live export containing real tenant data — app names, publisher domains, owners if `-IncludeOwners` was used) sits in the repo root. With no `.gitignore` and no git repo yet, this is worth deciding on deliberately before version control is introduced — timestamped exports will otherwise accumulate in source control.
4. **No dependency manifest.** The script declares required PowerShell modules (`Microsoft.Graph.Authentication`, `Microsoft.Graph.Applications`, `ImportExcel`) inline in code/comments, but there's no `.psd1`, `requirements.txt`, or equivalent lock file capturing versions. Not a problem for a single script, but worth setting up if `entra-scripts/` grows.
5. **`entra-scripts/` is empty** — purpose undocumented.
