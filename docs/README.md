# docs/ — Index

Standing index of everything in `docs/`. This is the **only** file that
should need an edit when a doc moves or a new one is added — nothing else
in the repo should hardcode paths into `docs/`.

| Topic | File |
|---|---|
| Learning loop workflow (how CLAUDE.md gets updated over time) | [IAM_SCOUT_LEARNING_LOOP.md](IAM_SCOUT_LEARNING_LOOP.md) |
| EntraID/PowerShell project plan (phases, scope, open questions) | [ENTRAID_POWERSHELL_PROJECT_PLAN.md](ENTRAID_POWERSHELL_PROJECT_PLAN.md) |
| Session log (raw, append-only findings) | [LEARNINGS.md](LEARNINGS.md) |

`docs/` is the permanent home for project documentation. Subdirectories may
be added over time (e.g. `status/` for dated snapshots) without requiring
changes anywhere outside this file.
